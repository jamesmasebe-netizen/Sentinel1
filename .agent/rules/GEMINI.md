# Global Project Rules

Canonical rules for this repo now live in [`.agents/AGENTS.md`](../../.agents/AGENTS.md). Read that file — it covers architecture, data modeling, UI, completeness standards, token-efficiency conventions, and agent workflow primitives.

This file is kept only so tooling that specifically looks up `.agent/rules/GEMINI.md` keeps resolving to the right place. Do not add rules here — edit `.agents/AGENTS.md` instead, so Antigravity/Gemini and Claude Code never drift apart.
