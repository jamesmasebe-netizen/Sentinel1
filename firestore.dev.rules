rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY DEV RULES — allows all reads/writes without auth.
    // MUST BE REVERTED BEFORE PRODUCTION!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
